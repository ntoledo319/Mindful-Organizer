# build/

electron-builder's `buildResources` directory.

Icon files (`icon.png`, `icon.ico`, `icon.icns`) are generated from
`resources/app-icon.png` by `npm run icons` and are intentionally not committed.
Run that script before packaging.
