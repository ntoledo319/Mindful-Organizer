# Core runtime dependencies for Mindful Organizer.
#
# NOTE: pyproject.toml is the canonical dependency source.
# Install in development with:  pip install -e ".[dev]"
# Install with ML features:     pip install -e ".[dev,ml,nlp]"
#
# This file is kept for backward compatibility with legacy install
# workflows (e.g. build_windows.bat). It lists only core deps.

PyQt6>=6.4.0
numpy>=1.24.0
cryptography>=41.0.0
psutil>=5.9.0
keyring>=24.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
httpx>=0.26.0
